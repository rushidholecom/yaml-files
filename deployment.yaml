apiVersion: apps/v1
kind: Deployment
metadata:
  name: rushi-deployment    
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rushi_app
  template: 
    metadata:
      labels:
        app: rushi_app
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80