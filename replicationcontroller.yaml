apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx-rc
spec:
  replicas: 3
  selector:
    app: rushi_app
  template:
    metadata:
      labels:
        app: rushi_app
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80